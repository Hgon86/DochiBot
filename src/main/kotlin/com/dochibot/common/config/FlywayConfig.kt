package com.dochibot.common.config

import org.springframework.context.annotation.Configuration

/**
 * Flyway 설정.
 *
 * Spring Boot가 자동 설정을 제공하지만,
 * 커스터마이징이 필요할 때 이 파일을 수정합니다.
 *
 * 기본 설정은 application.yml의 spring.flyway.*를 통해 관리됩니다.
 */
@Configuration
class FlywayConfig {
}
